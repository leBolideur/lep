var name = "Monkey";
var age = 1;
var inspirations = ["Scheme", "Lisp", "JavaScript", "Clojure"];

var book = {
    "title": "Writing A Compiler In Go",
    "author": "Thorsten Ball",
    "prequel": "Writing An Interpreter In Go"
};
var printBookName = fn(book):
    var title = book["title"];
    var author = book["author"];
    print(author + " - " + title);
end;

printBookName(book);

var numbers = [1, 1 + 1, 4 - 1, 2 * 2, 2 + 3, 12 / 2];
print("len: ");
len(numbers)
