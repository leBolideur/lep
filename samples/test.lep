fn great(name): print("hello, " + name); end

fn loop(times, func, name):
    if (times > 0):
        loop(times - 1, func, name);
    else:
        func(name);
    end
end

loop(10, great, "max");
var i = 4;
