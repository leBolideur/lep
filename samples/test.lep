fn great(name):
    print("Hello, ", name, "!");
end

var name = "world";

fn loop(times, i):
    if i < times:
        great("world");
        loop(times, i + 1);
    end
end

loop(3, 0);
loop(2, 0);
